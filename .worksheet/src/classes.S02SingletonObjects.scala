package classes

object S02SingletonObjects {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(90); val res$0 = 
  ChecksumAccumulator.calculate("ScalaBala");System.out.println("""res0: Int = """ + $show(res$0))}
}
